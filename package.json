{
  "name": "kdps",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sandcastle": "tsx .sandcastle/main.mts",
    "sandcastle:build": "sandcastle docker build-image --dockerfile .sandcastle/Dockerfile",
    "setup": "cd app/backend && uv sync && cd ../.. && npm --prefix app/frontend install",
    "ci": "npm run ci:backend && npm run ci:frontend",
    "ci:backend": "cd app/backend && uv run ruff format --check . && uv run ruff check . && uv run mypy core config && uv run python manage.py makemigrations --check --dry-run && uv run lint-imports && uv run pytest",
    "ci:frontend": "npm --prefix app/frontend run ci"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.5.10",
    "tsx": "^4.19.2"
  }
}
