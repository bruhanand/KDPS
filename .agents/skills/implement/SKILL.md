---
name: implement
description: "Phase 4 - take one ready issue to an open PR: implement, review once, live browser QA, push. Never merges."
disable-model-invocation: true
---

# Implement

`implement <issue#>`, or `implement next` for the oldest unassigned `ready` issue.

Read `docs/agents/phases/implement.md` and follow it.

It stops at an open PR with evidence. A human merges.
