---
name: design
description: "Phase 2 - turn an approved spec into one design document: endpoints with error tables and step flows, the schema delta, the postings, and the component breakdown."
disable-model-invocation: true
---

# Design

Read `docs/agents/phases/design.md` and follow it.

Output: `docs/features/<slug>/design.md`, which must pass the completeness gate in that document
before you show it. Stop there; phase 3 is `to-tickets`.

Skippable when the feature is not a money slice and fits in one or two tickets. Never skippable on a
money slice.
