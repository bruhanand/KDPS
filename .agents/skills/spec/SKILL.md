---
name: spec
description: "Phase 1 - map a feature's blast radius across the KDPS system, flag whether it is a money slice, then grill it to a spec. Invoke with everything known about a new feature."
disable-model-invocation: true
---

# Spec

Read `docs/agents/phases/spec.md` and follow it.

Output: `docs/features/<slug>/spec.md`. Stop there; phase 2 is `design`.
