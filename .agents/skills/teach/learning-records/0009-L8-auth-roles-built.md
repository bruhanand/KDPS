# Lesson 8 built — who is allowed (auth & roles), backend deep-dive 3 of 4

20 Jun 2026. Built `0008-who-is-allowed.html` + `reference/auth-roles-cheatsheet.html`, deep from the start per Anand's request. Covers: authentication vs authorization (the hotel ID-vs-room-key picture), the login flow + tokens (signed, carry user/role/store/expiry; ties to L6 stateless), roles & permissions / RBAC with a KDPS Cashier/Manager/Owner permission table, where checks run (gatekeepers: requireLogin + requireRole), never-trust-the-request's-role (req.user.role not req.body.role), screen-lock-is-fake (repeat of L7 trap), and **broken access control** (the #1 OWASP risk — the Store-7→Store-3 URL-edit example + data scoping for multi-store/two-state KDPS), plus hashed passwords and token expiry. 10-question quiz with deliberately tempting distractors (the "simplest/quickest/they-won't-try" traps).

**Backend track:** L6 ✅ · L7 ✅ · L8 ✅ · **next L9 — when things go wrong + background jobs** (errors, the 30/15/7-day alerts, Tally sync), the last of the 4. After that the deferred rungs remain: reading AI code, local-vs-live. Ties to D4 payments-and-roles. Style per [[NOTES]] holding well.
