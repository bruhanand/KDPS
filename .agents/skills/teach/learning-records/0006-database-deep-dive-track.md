# Database is the priority module — deep-dive track opened

20 Jun 2026. Anand named the **database** as the one area he most needs to master: "I want to understand enough that when I review code or review the schema, I know what's built and how it's built and what's going to happen and where it's going." He can already read a database a little but wants to get fast/confident. So the ladder is reordered to put a **database deep-dive** front and centre before resuming "reading AI code" and "going live."

**Built this session:**
- **Lesson 5 — Reading a query** (`0005-reading-a-query.html`): SELECT/FROM/WHERE/GROUP BY/SUM, reads-vs-writes, the no-WHERE danger. + `sql-reading-cheatsheet.html`.
- **The KDPS Database Handbook** (`reference/database-handbook.html`) — his explicit ask for ONE very detailed HTML to read end-to-end: 17 sections (big picture → types/money-NUMERIC → keys → reading a schema → constraints → SELECT → aggregates → JOINs → writes → indexes → transactions/ACID → migrations → normalisation/snapshots → the ledger pattern → KDPS schema map → reviewer's checklist → glossary). Sticky-TOC, print-friendly, grounded in KDPS tables.

**Planned remaining deep-dive lessons:** L6 JOINs · L7 migrations · L8 constraints & transactions/integrity. (The handbook already covers all of these in reading form; the lessons add the interactive quiz/practice layer.)

Hands-on option still open: `code/pdf-to-pt` (real Python project) to make any of this concrete. See [[0005-lesson-3-4-built]].
