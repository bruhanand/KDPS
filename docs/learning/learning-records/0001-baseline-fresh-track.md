# Baseline for the fresh engineer track (8 Jul 2026)

Anand starts the P0–P10 track with: strong retail domain mastery (he authored the KDPS design), real front-end experience (has built UIs), basic Python, and concept-level software knowledge from the retired 2026-06 track (four-box model, git-as-undo-button, SQL reading, request→response, auth roles — lessons 0001–0008 in `.agents/skills/teach/lessons/`). He cannot yet fluently read Django or TypeScript syntax.

**Implications:** never re-teach the *concepts* above — cite them as known and go straight to reading the *real code* for each. The zone of proximal development for P0 is "I know what an API is; I cannot yet open `views.py` and follow it." Depth targets come from `stack-learning-roadmap.html`: P2 and P8 are DEEP, money paths get the slowest pace.
